schema: "http://localhost:3000/api/graphql"
documents: "graphql/**/*.ts"
generates:
    graphql/generated.ts:
        plugins:
            - "typescript"
            - "typescript-operations"
        config:
            avoidOptionals: true
            nullableValues: undefined
